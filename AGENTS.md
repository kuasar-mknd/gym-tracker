# Instructions pour les agents

Les instructions du projet vivent dans `CLAUDE.md` (généré par Laravel Boost, une seule source)
et dans `.ai/rules/` (règles du dépôt). Ce fichier n'est qu'un renvoi : n'y copie aucune règle,
un test de convention le garde court.
